import { StyledSectionTitle } from "./styles/Common.styles";

const UITitle = ({title})=> {
    return (
        <StyledSectionTitle>
             <span>{title}</span>
        </StyledSectionTitle>
    )
}

export default UITitle;